# DPA Tools Quickstart Demo

Open `quickstart.ipynb` in Jupyter and run all cells top-to-bottom.
Runs on CPU in under 5 minutes with the 50 pre-processed molecules in `data/`.

To regenerate the demo data from raw GDB9, see `scripts/prepare_data.py`.
